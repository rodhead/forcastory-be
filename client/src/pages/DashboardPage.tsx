// src/pages/DashboardPage.tsx
// Redirect shell — beforeLoad in router handles the actual redirect.
// This component is a safety fallback and should never render.
export function DashboardPage() {
  return null
}
